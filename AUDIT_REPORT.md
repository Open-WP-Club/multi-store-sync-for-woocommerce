# Plugin Audit — Remove / Renew / Modernize


Work through this top to bottom, lowest-risk first. Run `composer test`
after each change before checking it off.

Completed items have been removed from this report to keep focus on what's
still undecided. See git history for the full record.

---

## REMOVE — dead code / unused flexibility

### Api/logging/webhook
- [~] `includes/webhook-logger.php` / `includes/deletion-audit.php` / `includes/api-usage-tracker.php` — `get_logs()` / CSV-export / `cleanup_old_logs()` / `csv_cell_sanitize()` are near-identically reimplemented three times (WHERE-builder, pagination, CSV formula-injection guard). Extract a shared trait/base class. _(partial — `csv_cell_sanitize()` was the only piece that was actually byte-identical, so that's what got extracted, into new `includes/csv-sanitize-trait.php` (`WC_Multi_Store_Csv_Sanitizer`), used by `webhook-logger.php` and `api-usage-tracker.php`; `deletion-audit.php` has no CSV export at all, so it doesn't need the trait. **Left `get_logs()`/pagination and `cleanup_old_logs()` alone on closer read**: their WHERE-builders filter different columns with different match types (webhook-logger's `LIKE` partial match vs deletion-audit's exact `=`), their cleanup methods differ in date-math approach (PHP `strtotime()` vs SQL `DATE_SUB`), logging side effects (conditional/unconditional/none), and return shapes (webhook-logger's pagination wrapper vs deletion-audit's raw array + separate `get_total_count()` vs api-usage-tracker has no `get_logs()` equivalent at all). A shared base would need a config object to paper over those differences — the "config for values that never change" trap, not a real dedup.)_

### Settings/reporting
- [x] `includes/queue-table.php:60,75-92` — `maybe_upgrade_table()` hand-rolled `ALTER TABLE ADD COLUMN` checks for columns `dbDelta()` (called right above with the same columns in the CREATE TABLE SQL) already adds. **Removed** — `dbDelta()` is the documented WP mechanism for adding columns to an existing table on schema change; the manual `SHOW COLUMNS`/`ALTER TABLE` duplication added no coverage `dbDelta()` doesn't already provide. Plugin owner's call, no staging verification needed.

---

## RENEW — outdated approach vs current WP/WC/PHP standard

- [~] `includes/api-client.php:254-338` vs `includes/webhook-receiver.php:45-77` — two independent rate-limiter implementations reinvent the same `wp_cache_add`+`wp_cache_incr`/transient-fallback technique. Extract one shared helper. **Reviewed, not touched**: these serve different purposes with different precision needs — `api-client.php` self-throttles the plugin's own *outgoing* API calls and needs to compute an exact wait-time (true sliding window merging in-memory + transient timestamps in its fallback path), while `webhook-receiver.php` rejects *incoming* requests over a threshold with a simple boolean/count (its fallback is a much simpler non-atomic counter). Only the object-cache fast path's 2-line `wp_cache_add` + `wp_cache_incr` idiom is genuinely identical; extracting a shared helper for just that would save 2 lines twice while the two functions' surrounding logic (and fallback behavior) stay completely different — marginal enough that it isn't worth a new shared primitive.
- [~] `includes/circuit-breaker.php:146-156` — independently reimplements transient-based state storage rather than extending `Cache_Manager`. Third parallel transient-cache implementation for the same pattern. **Reviewed, not touched**: unlike the sync-engine.php term cache (genuinely stale-data-risk caching of remote API responses — Cache_Manager's stated domain), circuit-breaker state is *protective*, not a performance cache — it deliberately should NOT be swept by a generic "clear all caches" admin action (that would resume hammering a store that's circuit-broken for a good reason; there's a separate explicit `reset()` for that). It also needs a MySQL advisory lock around its read-modify-write that `Cache_Manager` doesn't provide and has no reason to. Routing it through `Cache_Manager` would mean either adding a valueless generic pass-through method, or making protective state sweepable by a blanket cache-clear action — the latter would be a behavior regression, not a cleanup.
- [x] `includes/category-mapper.php`, `attribute-remapper.php`, `shipping-class-sync.php`, `downloadable-files-sync.php` — each kept settings under its own `get_option()` key instead of the central `WC_Multi_Store_Settings` store. **Done** — the `enabled`/`auto_sync_on_change`/`transfer_mode` flags for all 4 classes now live in the central store as `{prefix}_enabled` etc. (e.g. `shipping_class_sync_enabled`), consistent with `stock_sync_enabled`/`image_proxy_enabled` already there. The one-time `migrate_settings_to_central_store()` migration (plus its `maybe_upgrade_database()` call site and the legacy `SETTINGS_KEY` constants) has since been removed — plugin owner's call, no need to carry it further.
  **Not migrated — left as a separate, harder problem**: `category-mapper.php`'s and `attribute-remapper.php`'s actual mapping-data options (`wc_mss_category_mappings`, `wc_mss_tag_mappings`, `wc_mss_attribute_name_mappings`, `wc_mss_attribute_value_mappings`) are keyed by `md5($store_url)`, while the central store's per-store precedent (`pricing_rules`/`stock_allocation_rules` as sub-keys of each store's config in `wc_multi_store_sync_stores`) is keyed by literal store URL. Moving these would mean re-deriving `md5(url) → url` for every currently-known store to re-key the data, with no clean answer for orphaned entries belonging to since-deleted stores.

  **Follow-up, resolved**: the AJAX endpoints that write/read this mapping data (`ajax_save_mappings()`/`ajax_get_mappings()` on both classes, `wp_ajax_wc_mss_save_category_mappings`/`wp_ajax_wc_mss_get_category_mappings`/`wp_ajax_wc_mss_save_attribute_mappings`/`wp_ajax_wc_mss_get_attribute_mappings`, all registered in `class-admin-ajax.php`) are live, nonce- and capability-checked, and fully functional — but genuinely have no caller anywhere in `admin/js/admin-scripts.js` or any `admin/views/*.php`. Root cause: `settings.php`'s "Category Mapping" toggle description said _"Configure mappings in each store's settings"_, pointing at a per-store mapping screen that was never built — the toggle enables a feature with no way to actually enter mapping data. Fixed the misleading copy on both the Category Mapping and Attribute Remapping toggles (plugin owner's call: fix the description, leave the endpoints in place for a future UI rather than removing them or building that UI now) — both now say plainly that there's no admin screen yet to enter mappings, so enabling the toggle alone does nothing.
- [~] `md5($store_url)` independently re-derived as a cache/meta-key fingerprint in `product-extractor.php`, `category-mapper.php`, `attribute-remapper.php`, `product-edit.php`, `remote-product-manager.php`. One shared `StoreKey` helper would remove the repetition. **Reviewed, not touched**: actually appears in ~15 files, not the 5 named. A wrapper class/method around a single stdlib call (`md5($x)`, 11 characters) would replace it with a longer static-method call everywhere — less lazy, not more, and doesn't reduce real complexity. Only worth revisiting if the hashing scheme itself ever needs to change (e.g. to sha256), which is speculative, not a current need.
- [~] `admin/views/dashboard.php:147-303` + `admin/views/weekly-verification.php:431-591` — ~150-line inline `<script>` for verification start/poll/cancel. **Reviewed, not touched**: not near-identical on closer read — deliberately different UX per context. Idle-poll timeout is 5×3s (~15s) in dashboard.php vs 40×3s (~2min) in weekly-verification.php; on `completed` dashboard.php resets the UI while weekly-verification.php calls `window.location.reload()`; on idle-exceeded dashboard.php resets + shows an error while weekly-verification.php leaves the progress bar visible (orange, cancel hidden) without resetting; weekly-verification.php has an extra `pending` status branch dashboard.php doesn't; `startVerification()` has a `.catch()` in dashboard.php but not in weekly-verification.php; button/status label text differs throughout. A shared helper would need a config object for 10+ divergent behaviors — the same "config for values that never change" trap already flagged elsewhere in this doc — and there's no JS test coverage to catch a regression in either flow. Left as two intentionally-different implementations.

---

## MODERNIZE — PHP 8.4 is already the minimum version; use its features consistently

- [~] `includes/queue-manager.php:18-21` — `PRIORITY_CRITICAL/HIGH/NORMAL/LOW` bare int constants (1/2/3/10). **Not converted to backed enum**: the `priority` column is an ordinal 1-10 scale, not a closed set — `queue-table.php::add()` defaults `$priority = 5` and branches on `$priority < 5` (line 164) to decide whether to bump an existing queue item's priority; `5` isn't one of the four named constants, and `get_next_batch()` sorts with plain `ORDER BY priority ASC`. Every call site (`queue-manager.php`, `queue-table.php`) types `$priority` as bare `int`, and reflection-based tests (`QueueManagerTest.php:189`) assert `getDefaultValue()` against the raw int. A 4-case enum can't represent the full domain this column actually stores/compares — the named constants are presets within a wider ordinal scale, not the type itself.


Три паралелни агента минаха през целия plugin (~32k реда в includes/, admin/views, root файлове). Ето обединения резултат — само over-engineering/bloat находки (без performance/security, това е извън обхвата на този одит):

Най-големи находки

- yagni Category/Attribute Mapping системата (CRUD, AJAX handlers, apply_mappings/apply_tag_mappings/apply_variation_mappings) е напълно недостижима — самият settings.php признава "there is currently no admin screen to enter mappings — enabling this alone has no effect." Изтрий фийчъра или довърши UI-то. includes/category-mapper.php, includes/attribute-remapper.php (~773 реда)
- [x] fix (не delete) `includes/email-notifications.php` — **Не беше чист dead code, а счупен feature**: failed_sync/api_error/low_stock имаха enable toggle-и в Settings, но нищо не викаше `trigger_sync_failed()`/`trigger_api_error()`/`trigger_low_stock()`. **Свързано**: `queue-manager.php`'s process-queue loop сега вика `trigger_sync_failed()` на всеки retryable failure и `trigger_api_error()` когато circuit breaker-ът се отвори (устойчив API проблем, не единичен failure); `product-transformer.php::apply_store_rules()` вика `trigger_low_stock()` за всеки store след stock allocation (rate-limit-нато от `claim_daily_slot()` вътре в `send_low_stock_notification()`, safe за unconditional call). Тествано в `QueueManagerProcessTest.php`/`ProductTransformerTest.php`.
- [x] fix (не delete) `includes/performance-monitor.php` — единственото извикване (bulk-actions.php) старт/стоп-ваше таймер и никога не четеше резултата. **Изтрит изцяло** (клас + двата call site-а в bulk-actions.php) — чист dead code, нямаше settings toggle или друга видима за потребителя връзка.
- shrink Идентичен ~155-редов polling <script> блок (start/poll/cancel/reset) copy-paste между dashboard.php и weekly-verification.php. Един параметризиран JS helper. admin/views/dashboard.php:147-302, admin/views/weekly-verification.php:424-585
- [x] fix (не delete) Downloadable-files "API transfer mode" — **три отделни бъга**, не просто мъртъв код: (1) `sync_downloads()` четеше грешен settings key (`download_files_transfer_enabled`, никога дефиниран) вместо `transfer_mode`; (2) `upload_file_to_remote()` викаше `WC_Multi_Store_Image_Proxy::upload_media_to_remote()` — метод, който изобщо не съществува; (3) `sync_downloads()` никога не се викаше от реалния sync path. **Оправени и трите**: fixed settings key; upload сега минава през `$client->upload_image()` (същия wp/v2/media endpoint, вече използван за product images) през временен файл; `sync-engine.php::sync_product_to_store()` вика `sync_downloads()` след `apply_store_rules()` за downloadable продукти. Тествано end-to-end в `DownloadableFilesSyncTest.php`/`SyncEngineSyncProductToStoreTest.php`.

Дублирана логика/мъртви делегати

- delete Remote_Product_Manager::delete()/restore()/update_status() и Variation_Synchronizer::delete_variation() — sync-engine.php реимплементира същата логика inline, вместо да ги вика. includes/remote-product-manager.php:214,273,341, includes/variation-synchronizer.php:325
- shrink pricing-rules.php — 4 метода (apply_fixed/percentage/multiplier_adjustment, apply_currency_conversion) с идентичен 15-редов loop → един apply_adjustment()
- shrink deletion-audit.php get_logs()/get_total_count() строят еднакъв WHERE clause 2 пъти; същото за category-mapper.php (apply_mappings/apply_tag_mappings), remote-order-table.php (3×), sync-history.php (3×), api-client.php batch-size проверки (4×)
- yagni Купчина one-line delegate wrapper-и с единствен caller: webhook-receiver.php::get_client_ip(), remote-order-sync.php::create_api_client(), pricing-rules.php::apply_to_variation(), weekly-verification-comparator.php (2 wrapper-а) — обади се директно на подлежащия метод, изтрий wrapper-а
- shrink coupon-sync.php/shipping-class-sync.php — schedule_async() и get_api_client() дефинирани идентично в двата файла
- native remote-order-list-table.php hand-rolled $status_labels map (дефинирана 2 пъти в файла!) вместо wc_get_order_status_name(); order-sync.php hand-rolled static settings cache вместо директно get_option()

Мъртъв код (unused public methods, никой ги вика извън тестове)

time-manager.php::get_settings_info()/estimate_sync_time() · product-exclusion-filter.php::is_category_excluded()/is_tag_excluded() · stock-allocator.php::get_allocation_types() + pricing-rules.php::get_rule_types() · store-health-check.php::get_health_summary()/get_last_health_check() · api-usage-tracker.php::cleanup_old_data() (никога не се закача към daily maintenance) · dead-letter-queue.php::cleanup() (същото) · order-sync.php::sync_last_orders()/get_statistics() · category-deletion-sync.php::get_settings()/update_settings() (дублира WC_Multi_Store_Settings) · coupon-sync.php::delete_coupon_from_all_stores() · conflict-detector.php::get_unresolved_conflicts() · category-mapper.php::add_mapping()/remove_mapping()/get_remote_categories()

Мъртви config флагове и write-only данни

conflict-detector.php action_on_conflict/notify_email (записват се, никой не ги чете) · coupon-sync.php auto_sync_on_save/auto_sync_deletions · api-usage-tracker.php cost-estimate опции (wc_mss_api_cost_per_1000/gb) — няма settings поле, "Cost Estimates" картата винаги е $0 · pricing-rules.php currency_from/currency_to полета никога не се четат · product-edit.php пише _wc_mss_deletion_stores_map post meta, никой не го чете · dead-letter-queue.php::do_action('wc_mss_dead_letter_added') — "no consumers anywhere" по собствените коментари в кода

Плюс: неизползвани локални променливи (variation-synchronizer.php:249-251, api-usage.php:33, discrepancies.php:68), излишен in-memory+transient двоен cache layer в settings.php::get_active_stores() (файлът сам коментира, че вторият слой "would only add an invalidation path for no real benefit"), излишен cache-manager.php warmup() hook.

net: приблизително -1800 до -2000 реда (от ~32 600 в includes/), 0 deps.

---⚠️ Извън обхвата на тоя одит, но си струва да знаеш — двe находки всъщност бяха бъгове, не bloat. **И двете оправени**:
1. [x] orphan-cleanup.php:197-266 — Delete-Selected бутонът викаше $api_client->make_request(), метод който не съществуваше в WC_Multi_Store_API_Client (реален клик там → fatal error). **Оправено**: сега използва `WC_Multi_Store_API_Client::for_store($store_url, $config)->delete_product($product_id, true)`, същия pattern като останалия sync код. Тествано в `OrphanCleanupTest.php` (реални `wp_remote_request` stub-ове вместо mock на несъществуващия метод).
2. [x] sync-previewer.php:227-250 — apply_pricing_rules()/apply_stock_allocation() викаха apply_store_pricing()/allocate_stock(), методи с други имена/сигнатури в реалния API (preview редовете за pricing/stock никога не работеха). **Оправено**: сега вика `WC_Multi_Store_Pricing_Rules::preview_price()` и `WC_Multi_Store_Stock_Allocator::calculate_allocation()` — реалните static методи. Тествано в `SyncPreviewerTest.php`.

Искаш ли да пристъпя към прилагане на останалите находки (или само на топ 5-те най-безопасни)?